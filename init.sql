CREATE TABLE IF NOT EXISTS `hashdb-1`
(
    hash    VARCHAR(64)   NOT NULL,
    url     VARCHAR(4096) NOT NULL,
    login   BOOLEAN       NOT NULL,
    created BIGINT        NOT NULL,
    PRIMARY KEY (hash)
);

CREATE TABLE IF NOT EXISTS `hashdb-2`
(
    hash    VARCHAR(64)   NOT NULL,
    url     VARCHAR(4096) NOT NULL,
    login   BOOLEAN       NOT NULL,
    created BIGINT        NOT NULL,
    PRIMARY KEY (hash)
);

CREATE TABLE IF NOT EXISTS `hashdb-3`
(
    hash    VARCHAR(64)   NOT NULL,
    url     VARCHAR(4096) NOT NULL,
    login   BOOLEAN       NOT NULL,
    created BIGINT        NOT NULL,
    PRIMARY KEY (hash)
);

CREATE TABLE IF NOT EXISTS `hashdb-4`
(
    hash    VARCHAR(64)   NOT NULL,
    url     VARCHAR(4096) NOT NULL,
    login   BOOLEAN       NOT NULL,
    created BIGINT        NOT NULL,
    PRIMARY KEY (hash)
);

CREATE TABLE IF NOT EXISTS `hashdb-5`
(
    hash    VARCHAR(64)   NOT NULL,
    url     VARCHAR(4096) NOT NULL,
    login   BOOLEAN       NOT NULL,
    created BIGINT        NOT NULL,
    PRIMARY KEY (hash)
);

CREATE TABLE IF NOT EXISTS `hashdb-6`
(
    hash    VARCHAR(64)   NOT NULL,
    url     VARCHAR(4096) NOT NULL,
    login   BOOLEAN       NOT NULL,
    created BIGINT        NOT NULL,
    PRIMARY KEY (hash)
);
